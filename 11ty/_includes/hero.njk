<section id="hero">

  <article>

    {% if section %}
      <h2>{{ section }}</h2>
    {% endif %}

    {% if quote %}
      <h3>{{ quote }}</h3>
    {% endif %}

    {% if description %}
      <p>{{ description }}</p>
      <a href="#contact">Let's build something together</a>  
    {% endif %}
  
  </article>
  
</section>
