---
uuid: bae421fa-aad9-458b-a73e-8e24f60d3c1b
permalink: /notes/2026/05/02/
excerpt: "When you are presenting at a conference, the last thing you want to show is your messy desktop. Files everywhere. Maybe some revealing. I use this little shell script on MacOS to hide all the files on the desktop. Run it againt to bring them back. Also good for recording videos."
code: true
image: false
link: false
video: false
name: "code"
---

When you are presenting at a conference, the last thing you want to show is your messy desktop. Files everywhere. Maybe some revealing. I use this little shell script on MacOS to hide all the files on the desktop. Run it againt to bring them back. Also good for recording videos.

``` bash
#!/bin/bash
file="present.txt"

if [ -f "$file" ]
then
	rm $file
	defaults write com.apple.finder CreateDesktop true
	killall Finder
else
	echo "You got this." > $file
	defaults write com.apple.finder CreateDesktop false
	killall Finder
fi
```

``` bash
~/present.sh
```