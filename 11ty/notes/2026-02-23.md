---
uuid: 855679d7-cbb1-4c0b-b16d-96a2e2012236
permalink: /notes/2026/02/23/
excerpt: "Not perhaps as equally as impressive as the color-mix function, but sometimes you need to fill a span of text with a gradient color. Not the background; the text itself. The tricky part here is in getting the clipping properties set correctly. CSS is so powerful these days."
code: true
image: false
link: false
video: false
name: "code"
---

Not perhaps as equally as impressive as the `color-mix` function, but sometimes you need to fill a `span` of text with a gradient color. Not the background; the text itself. The tricky part here is in getting the clipping properties set correctly. CSS is so powerful these days.

``` css
.gradient-text {
  background: 
    linear-gradient(
      90deg, 
      var( --brand-purple ), 
      var( --brand-pink )
    );
  background-clip: text;  
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
```
