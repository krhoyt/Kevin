---
uuid: d28ed3c1-2577-46c6-ae64-68ec360cc4d0
permalink: /notes/2026/06/19/
excerpt: "When exporting SVG from Illustrator, individual tspan elements will be created for text blocks with font changes. The spans include coordinate attributes, preventing the natural flow of the text. This can create unwanted spacing between words. These attributes can be removed."
code: true
image: false
link: false
video: false
name: "code"
---

When exporting SVG from Illustrator, individual tspan elements will be created for text blocks with font changes. The spans include coordinate attributes, preventing the natural flow of the text. This can create unwanted spacing between words. These attributes can be removed.

``` js
function stripPosition( tag ) {
  return tag
    .replace( /\s+x="-?[\d.]+"/g, '' )
    .replace( /\s+y="-?[\d.]+"/g, '' )
    .replace( /\s+xml:space="[^"]*"/g, '' );
}
```
