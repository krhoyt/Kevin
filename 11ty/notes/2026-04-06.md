---
uuid: 5568775c-b677-47f0-b7c8-76d94256bc5f
permalink: /notes/2026/04/06/
excerpt: "Handling the click event is common in web development. Tablets and phones, do not technically have click but will respond to the event at the end of a touch. Capturing touchstart can lead to improved perceived performance on these devices. Here is how to handle both."
code: true
image: false
link: false
video: false
name: "code"
---

Handling the `click` event is common in web development. Tablets and phones, do not technically have `click` but will respond to the event at the end of a touch. Capturing `touchstart` can lead to improved perceived performance on these devices. Here is how to handle both.

``` js
const touch = ( 'ontouchstart' in document.documentElement ) ? 'touchstart' : 'click';

const button = document.querySelector( 'button' );
button.addEventListener( touch, () => console.log( 'Do something' ) );
```
