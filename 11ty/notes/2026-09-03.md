---
uuid: ddce2bbe-7426-4e71-9d6a-a755e1f27c0c
permalink: /notes/2026/09/03/
excerpt: "Building an MCP App? Do not use <base href> to point a resource's root-relative script src at your CDN. Claude Desktop hardcodes base-uri self and silently ignores your CSP's baseUriDomains - the tag just gets blocked, no error thrown. Rewrite the URLs to absolute instead."
code: true
image: false
link: false
video: false
name: "code"
---

Building an MCP App? Do not use `<base href>` to point a resource's root-relative script `src` at your CDN. Claude Desktop hardcodes `base-uri` `self` and silently ignores your CSP's `baseUriDomains` - the tag just gets blocked, no error thrown. Rewrite the URLs to absolute instead.

``` js
function absolutizeRootRelativeUrls( html, origin ) {
  return html.replace( /((?:src|href)=")\/(?!\/)/g, `$1${origin}/` );
}
```
